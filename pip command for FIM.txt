# pip install commands for the required libraries

pip install os-sys
pip install logging
pip install hashlib
pip install time
pip install openpyxl
pip install shutil
pip install Pillow
pip install python-docx
pip install uuid
pip install PyPDF2
pip install python-pptx
pip install watchdog
pip install schedule
pip install threading
pip install datetime
pip install glob3
